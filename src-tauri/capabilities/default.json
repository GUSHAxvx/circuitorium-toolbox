{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "工具箱默认能力：主窗口基础权限（前端只用 IndexedDB 与 blob，不需要额外插件）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
