# CIRCUITORIUM 课程演示指南

> 适用于课堂展示、课程设计答辩、学生上手体验等场景。

## ⚡ 一分钟快速启动

双击项目根目录的 **`启动.bat`**，它会自动：

1. 启动项目服务（第一个黑色窗口，不要关闭）
2. **等端口真正就绪后**才打开浏览器（不会再有「无法访问此网站」的假失败）
3. 启动**公网隧道**（第二个窗口显示 `https://xxx.trycloudflare.com` 链接）

隧道建立后会自动把地址**复制到剪贴板**，并写入项目根目录的 **`当前公网地址.txt`**，方便发群或贴进课件。把这个链接发给任何人，**不需要同一 WiFi** 即可访问你的项目（含手机摄像头、扫码克隆全功能）。

> 隧道断线会**自动重连**（每 3 秒试一次），不用管它；重连后网址会变，以窗口里最新显示的那一行为准。
> 只想开隧道时：先跑 `启动.bat` 或 `npm run dev`，再双击 **`仅启动隧道.bat`**。

## 一、启动服务（手动方式）

```bash
# 开发模式（改动即时生效）
npm run dev

# 或生产模式（更稳定、带 PWA Service Worker）
npm run build && npm start
```

启动后浏览器打开 <http://localhost:3000>（本机投影演示用这个地址即可）。

## 二、学生手机访问（同一 WiFi）

1. 电脑和学生手机连接**同一个 WiFi/热点**
2. 查询电脑局域网 IP：终端执行 `ipconfig`，找「IPv4 地址」（如 `192.168.x.x`）
3. 学生手机浏览器打开 `http://<电脑IP>:3000`（服务启动时也会打印 Network 地址）

**手机拍照说明：**

- 通过 `http://局域网IP` 访问时，浏览器内取景（WebRTC）会被浏览器禁止（要求 HTTPS），**但「拍照识别」按钮会自动改用系统相机**，拍照后自动识别——演示不受影响
- 需要完整的浏览器内取景/全屏 PWA 体验时，套一层 HTTPS 隧道即可（见下节）

## 三、可选：HTTPS 隧道（解锁手机摄像头预览 + PWA 完整安装）

推荐用 cloudflared（Cloudflare 免费隧道），**项目已内置**，在 `.tools\cloudflared.exe`，无需另装：

```bash
# 一键方式（推荐）：自动重连 + 自动抓取并复制地址
双击 仅启动隧道.bat

# 手动方式
.\.tools\cloudflared.exe tunnel --url http://localhost:3000 --no-autoupdate
```

学生访问该 HTTPS 地址即可获得全部能力（浏览器取景、安装到主屏幕）。

> 免费隧道地址每次重启都会变。若需要**固定网址**（例如要提前把二维码印在讲义上），请用 `部署指南.md` 里的 Docker + 服务器方案，或 cloudflared 命名隧道（需要自己的域名）。
> 备用隧道：`.tools\cloudflared.exe` 不存在时，脚本会自动回退到 localhost.run（`https://xxx.lhr.life`）。

## 四、安装为 App（PWA）

- **安卓（Chrome）**：打开站点 → 菜单「⋮」→「添加到主屏幕 / 安装应用」→ 桌面出现 CIRCUITORIUM 图标，点击全屏运行
- **iOS（Safari）**：分享按钮 →「添加到主屏幕」
- 注：完整安装能力需要 HTTPS（localhost 或隧道地址均可）；局域网 http 下仍可添加为桌面快捷方式

## 五、演示账号（一键进入）

- 登录页点击「🎓 课程演示：使用演示账号一键进入」
- 系统自动创建演示账号 `demo_teacher`，并生成一个**含完整教程、已公开分享**的演示项目「LED 呼吸灯（课程演示）」
- 首次进入会直接打开该项目；该演示项目同时出现在社区广场，可用来演示社区/克隆流程

## 六、推荐演示剧本（约 5 分钟）

1. **开场（投影）**：首页 Hero「从一个元器件，到完成一个项目」——介绍平台定位（电子工程项目工作台）
2. **认识**：点首页「AI识别元器件」→ 用实物或图片识别一个元件，展示 AI 生成的完整元件档案（型号/封装/引脚/规格）
3. **跟着做**：导航「模板」→ 打开「LED 呼吸灯」→ 展示图文步骤、接线说明、注意事项、BOM 清单 → 一键创建项目
4. **整理**：在项目页演示 顶部信息区（作者/时间/浏览量/收藏）→「项目预览」标签页与缩放 →「项目信息」栏 → 底部四卡（项目描述 / 元器件清单 BOM / 项目图片 / 相关项目推荐）
5. **分享（高潮）**：点「分享」→ 弹层出现二维码 → 投影二维码 → 学生扫码打开分享页 → 点「🚀 克隆到我的项目」→ 全班同时拥有该项目
6. **社区**：首页「社区项目」或导航「社区」展示公开项目广场（首页不显示克隆数/排行，排行榜只保留在社区页）

## 七、注意事项

- **AI 配额**：识别调用千问 VL（DashScope），连续演示注意配额消耗；`.env` 中可切换 `AI_PROVIDER=baidu` 走百度兜底
- **数据目录**：所有数据存于本地 `data.db` 与 `public/uploads/`，演示前可备份，演示产生的数据可随时删除
- **端口占用**：3000 被占用时改 `npm run dev -- -p 3001`（学生访问地址同步改端口）
- **防火墙**：学生访问不了时，检查 Windows 防火墙是否放行 Node（首次运行一般会弹窗询问，点允许）

## 八、常用命令速查

| 需求 | 命令 |
|---|---|
| 启动开发服务 | `npm run dev` |
| 生产构建 + 启动 | `npm run build && npm start` |
| 代码检查 | `npm run lint` |
| 查看本机 IP | `ipconfig` |
| HTTPS 隧道（自动重连） | 双击 `仅启动隧道.bat` |
| HTTPS 隧道（手动） | `.\.tools\cloudflared.exe tunnel --url http://localhost:3000` |

## 九、项目详情页导览（重构后）

打开方式：导航「项目」→ 点任一项目卡片；或从「社区」点开别人分享的项目。

页面结构（自上而下）：

| 区块 | 内容 | 说明 |
|---|---|---|
| 面包屑 | 社区 › 项目详情 | 一级入口可点 |
| 标题区 | 项目名 / 作者 / 发布时间 / 浏览量 / 收藏 / 标签 | 作者本人点标题可直接改名、点简介可编辑 |
| AI 识别环 | 元器件识别准确率 | 取该项目所有元器件的平均置信度；模板项目没有识别数据时显示「—」 |
| 项目预览 | 标签页：项目预览 / 电路图 / 原理图 / 实物图 / 3D 视图 | 支持 25%–300% 缩放、适应窗口、点击图片全屏查看；3D 视图暂为占位 |
| 项目信息 | 作者 / 发布时间 / 项目类型 / 使用元器件 / 文件大小 / 项目热度 / 项目简介 / 适用场景 | 项目类型与适用场景由已有内容自动推导（不改数据库） |
| 项目描述 | 简介 + 「主要功能」清单 | **作者可点右上角「编辑」自主填写**：描述正文 + 主要功能（每行一条）；没填时自动回退显示项目简介与教程小节标题 |
| 元器件清单（BOM） | 元器件（缩略图）/ 型号规格 / 数量 / 识别置信度 / 状态 | 本人点任意一行可就地编辑、改数量、加注解、移除；状态列为「已备齐 / 待备齐」可点切换 |
| 项目图片 | 大图 + 3 张缩略图 + 「查看全部 N 张图片」 | 展开后可分类筛选（原理图/电路图/接线图/实物图）、上传、删除 |
| 相关项目推荐 | 社区里其它公开项目 | 右侧 ☆ 可直接收藏 |
| 图文教程 | 制作步骤 / 注意事项 | 从模板创建的项目会自动带教程内容 |

按钮差异：

- **作者本人**：`分享项目`（弹层里是二维码 + 复制链接 + 取消分享）+ `⋯`（查看分享页 / 取消分享 / 删除项目）
- **访客（已公开分享的项目）**：`导入项目`（一键克隆到自己账号）+ `收藏` + `⋯`

> 说明：「浏览量」每次打开项目详情页 +1；「收藏」是新增的真实数据（`project_stars` 表），点一下即写入。

## 十、本地工具箱（新的传播方式）

网址：`/toolbox` —— 不登录、不联网、不需要任何配置就能用；项目、元件、图片、教程全部存在本机。

### 学生怎么拿到一个作品（三种任选）

| 方式 | 操作 |
|---|---|
| **分享页**（推荐） | 项目页点「分享作品」→「生成分享页」→ 得到一个 HTML 文件，发给学生；学生**双击就能看**（不用装工具箱、不用联网），页面里点「保存到我的工具箱」拿到作品文件，再在工具箱点「打开作品」选它 |
| **作品码** | 体量小的作品可生成二维码，投屏后学生扫码 → 把码粘到工具箱首页的「作品码」框 → 打开 |
| **作品文件** | 直接发 `.ecp` 文件，学生用「打开作品」选它 |

拿到之后点「做同款」就是自己的副本，随便改，改完还能继续分享给下一个人。

### 拍照识别（可选，不配也能用）

工具箱 → 右上角「识别设置」：默认「手动录入」（零配置，手动填元件）；想拍照识别可以选「本机配置」或「老师配置码」（部署见 `代理部署说明.md`）。没配置 **不影响** 其它任何功能。

### 便携版（发一个文件夹给学生，解压就能用）

在项目里执行一次 `npm run build:portable`，会生成 `便携版/`（约 1.3 MB）：

- 把 **整个 `便携版` 文件夹**拷给学生（U 盘 / 网盘 / 微信都行）
- 学生双击里面的 **`启动.bat`** → 浏览器自动打开工具箱
- 不需要装 Node、不需要联网、不需要管理员权限；数据存在学生这台电脑上
- 注意：拷给学生的是**程序**，不含你本机的项目与图片；作品要单独用「分享作品」发

### 分享页（学生扫码打开的那个页面）

分享链接 `…/share-project/<token>` 打开的是**同一套界面**（同一个 `ProjectDetailView` 组件），区别只有：

| | 项目详情页 | 分享页 |
|---|---|---|
| 访问地址 | `/projects/<id>` | `/share-project/<token>` |
| 是否需要登录 | 需要 | **不需要**（公开分享的项目） |
| 顶部按钮 | 分享项目 / ⋯ | **导入项目** / 收藏 / ⋯ |
| 编辑操作 | 可改名、改简介、改项目描述、改 BOM、传图 | 全部只读 |
| 未登录时 | 提示去登录 | 正常浏览；点「导入项目」跳登录页 |

> 也就是说：你在项目页怎么排版的，学生扫码看到的就是什么样子，不会再出现"两边不一样"。
