# 识别代理部署说明（给老师看）

> 目的：学生机器上**不需要任何 Key**，也能用「拍照识别元件」。真 Key 只放在你的云函数里。

## 一、什么时候需要这个

工具箱有三种识别方式，**任何一种都能正常使用工具箱的全部功能**，识别只是可选加速：

| 方式 | 谁需要动手 | 说明 |
|---|---|---|
| 手动录入（默认） | 谁都不用 | 自己填元件名称/型号/数量，所有本地功能都能用 |
| 本机配置 | 每个学生各填一次 | 学生自己申请 Key 填进去，只存在他自己电脑上 |
| **老师配置码** | 老师部署一次 | 学生扫个码就能用，机器上没有真 Key（推荐课堂用） |

> 浏览器里直连第三方识别服务可能被跨域限制挡住；走代理（本说明的方案）最稳。

## 二、部署步骤（约 10 分钟）

1. 申请一个识别服务 Key（例：阿里云百炼/DashScope，开通 `qwen3-vl-plus` 多模态模型）
2. 打开 <https://workers.cloudflare.com>，新建一个 Worker（免费额度足够课堂使用）
3. 把项目里 `tools/worker-proxy.js` 的内容整段粘贴进 Worker，部署
4. 在 Worker 的 **Settings → Variables** 里加两个变量：

   | 变量名 | 值 | 类型 |
   |---|---|---|
   | `UPSTREAM_KEY` | 你的真 Key | **Secret（加密）** |
   | `ACCESS_TOKEN` | 自己定一串口令，如 `class-2026-abc` | 普通变量 |

   可选：`UPSTREAM_BASE`（换服务商）、`MODEL`（换模型）
5. 记下部署后的地址，例如 `https://circuitorium-ai.你的账号.workers.dev`

## 三、生成配置码发给学生

1. 打开工具箱 → 右上角「识别设置 · …」→ 选「老师配置码」
2. 在下半部分「我是老师」填：
   - 代理地址：`https://circuitorium-ai.你的账号.workers.dev`（**不要**带 `/chat/completions`）
   - 访问令牌：`class-2026-abc`
   - 模型名：`qwen3-vl-plus`
3. 点「生成配置码」→ 得到一串 `CT1-…` 的码 + 一个二维码
4. 把二维码投屏，学生用手机扫；或把码复制到群里让学生粘贴

## 四、学生那边怎么用

- **扫码**：手机扫码后把内容复制到工具箱「识别设置 → 老师配置码 → 粘贴」→ 点「应用配置码」
- **粘贴**：直接把码贴进同一个输入框
- 应用成功后，项目页会出现「拍照识别元件」按钮，拍一张照就能把元件自动加进清单

## 五、安全说明

- 配置码里**只有**代理地址、访问令牌、模型名——**没有真 Key**，可以安全公开
- 作品文件（`.ecp`）导出时会自动排除本机设置与凭据，实测搜不到 Key/令牌
- 代理只转发 `…/chat/completions`，其它路径一律拒绝；限制单次消息条数，避免被当成免费大模型用
- 令牌泄露或学期结束：改 `ACCESS_TOKEN` 重新生成配置码即可，旧码立即失效（不用换 Key）
- 成本：识别按量计费，学生共用你的配额；课堂演示前建议先跑通一次，并留意额度

## 六、故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 提示「凭据被拒绝」 | 令牌不对或已更换 | 重新生成配置码 |
| 提示「连不上识别服务」 | 地址写错/断网/被跨域挡 | 检查地址是否以 http(s) 开头、是否有网络 |
| 提示「没识别出元件」 | 照片模糊/元件太小 | 换清晰照片，或手动录入 |
| 测试连接通过但识别失败 | 模型不支持图片 | 换多模态模型（如 `qwen3-vl-plus`） |
