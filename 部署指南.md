# CIRCUITORIUM 部署指南（脱离个人电脑运行）

> 目标：把项目部署到云服务器，任何人任何网络都能访问，答辩/提交成品无需带着电脑。

## 〇、方案速览

| 方案 | 费用 | 备案 | 访问速度 | 推荐度 |
|---|---|---|---|---|
| 阿里云/腾讯云轻量服务器（学生优惠） | 约 ¥10/月，常有免费试用 | IP 直连**不需要备案** | 快 | ⭐⭐⭐ 国内首选 |
| Oracle Cloud 永久免费 VPS | 免费（注册需外币信用卡） | 不需要 | 一般 | ⭐⭐ 零预算首选 |

> 也考虑过「用免费内网穿透把本机暴露到公网」这条路，**本项目已放弃**：Cloudflare 免费快速隧道
> 在国内网络访问不到（详见第三节的实测记录），留着只会让人以为能用。

**结论**：买一台最便宜的学生机（或申请 Oracle 免费机），用本指南的 Docker 方案，半小时内完成部署。

---

## 一、部署总流程（Docker，适用于任何 Linux 服务器）

### 1. 准备服务器
- 系统选 **Ubuntu 22.04 / 24.04**（腾讯云/阿里云轻量应用服务器默认即可）
- 安全组/防火墙放行端口：**3000**（TCP）
- SSH 登录服务器

### 2. 服务器安装 Docker

```bash
# 官方一键脚本
curl -fsSL https://get.docker.com | sh
# 安装 docker compose 插件
apt-get install -y docker-compose-plugin
```

> 国内服务器若下载慢，可配置镜像加速：
> `sudo mkdir -p /etc/docker && echo '{"registry-mirrors":["https://docker.m.daocloud.io"]}' | sudo tee /etc/docker/daemon.json && sudo systemctl restart docker`

### 3. 上传项目到服务器

方式一（scp，Windows 用 PowerShell）：
```powershell
scp -r D:\circuitorium-toolbox root@服务器IP:/root/circuitorium
```
方式二（git）：把项目推到 GitHub/Gitee，服务器 `git clone`。

> 上传前请删除本地的 `node_modules`、`.next`、`data.db*`、`public/uploads` 以减小体积（.dockerignore 已排除这些，scp 仍会传）。

### 4. 配置密钥（重要）

把本地的 `.env` 文件放到服务器项目目录：
```bash
# 在服务器上 /root/circuitorium 下创建 .env，粘贴本地 .env 内容
nano .env
```
内容形如：
```
JWT_SECRET=...
AI_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen3-vl-plus
BAIDU_API_KEY=...
BAIDU_SECRET_KEY=...
```

### 5. 一键构建启动

```bash
cd /root/circuitorium
docker compose up -d --build
```
首次构建约 3-5 分钟。完成后：

```bash
docker compose logs -f   # 查看日志，出现 Ready 即成功
```

浏览器访问 `http://服务器IP:3000` —— **部署完成**。

### 6. 日常维护命令

```bash
docker compose restart      # 重启
docker compose down         # 停止
docker compose up -d --build  # 更新代码后重新构建
docker compose logs -f      # 看日志
```

---

## 二、数据迁移（把本地已有的数据带到服务器）

本地 `data.db`（用户/项目/模板数据）和 `public/uploads`（图片）可以整体搬到服务器：

```powershell
# 先在服务器停止容器
# docker compose down

scp D:\circuitorium-toolbox\data.db root@服务器IP:/root/circuitorium/data.db
scp -r D:\circuitorium-toolbox\public\uploads\* root@服务器IP:/root/circuitorium/uploads/

# 服务器上重启
# docker compose up -d
```

> 注意：compose 已把 `./data.db` 和 `./uploads` 挂载进容器，服务器上的这两个路径就是"数据库和图片的家"。

---

## 三、（可选）获得漂亮的 HTTPS 域名

IP+端口能访问，但一直用裸 IP 不够体面，而且**手机浏览器内摄像头取景要求 HTTPS**。两条路：

- **有域名**：给服务器配 Nginx/Caddy 反代 + 免费证书（Let's Encrypt），一劳永逸，推荐。
- **没有域名**：国内小服务器 + frp（需要另一台有公网 IP 的机器做中转），或用 Cloudflare 的**命名隧道**（要自己的域名）。

> ⚠️ **不要用 `cloudflared` 的免费快速隧道（`*.trycloudflare.com`）**：本项目实测过，隧道能建起来
> （`Registered tunnel connection`），但这个公网域名在国内网络**访问不到**（TCP 443 不通），
> 学生照样打不开；而且地址每次重启都变。相关脚本已从项目里移除。

---

## 四、免费/低价服务器申请指引

### 国内学生机（推荐，快且便宜）
1. 阿里云 → 搜索「学生机」/「飞天加速计划」；腾讯云 → 「云+校园」
2. 学生认证后一般 **¥9.9/月 或免费试用 3-6 个月**
3. 购买「轻量应用服务器」，系统选 Ubuntu 22.04，地域选离你近的
4. 控制台「防火墙」添加规则：协议 TCP，端口 3000，来源 0.0.0.0/0
5. **IP 直连不需要 ICP 备案**（只有绑定域名才需要）

### Oracle Cloud（永久免费，需外币信用卡注册）
1. 注册 Oracle Cloud 免费账户 → 创建「Always Free」VM（ARM 2C12G 或 AMD 2C1G）
2. 系统 Ubuntu，安全列表放行 3000 端口
3. 按本指南部署即可，无备案问题

---

## 五、安全注意事项

- `.env` 含 AI 密钥：**不要提交到 GitHub/Gitee 公开仓库**（已在 .gitignore 中排除）
- 若把代码传到公开仓库，先删除 `.env` 再推送
- 数据库 `data.db` 定期备份：`docker compose down && cp data.db data.db.bak`
- 默认端口 3000 无 HTTPS 时，密码/Token 走明文 HTTP——答辩演示可接受；长期使用请配 HTTPS 反代（见第三节）

---

## 六、常见问题

| 问题 | 解决 |
|---|---|
| 访问不了 `IP:3000` | 检查服务器安全组/防火墙是否放行 3000；`docker compose ps` 看容器是否 Up |
| 构建慢/失败 | 国内服务器配置 Docker 镜像加速；重跑 `docker compose up -d --build` |
| 首页打开但图片不显示 | 确认服务器 `uploads` 目录存在且容器挂载正确 |
| AI 识别失败 | 服务器上 `nano .env` 检查密钥；`docker compose logs -f` 看报错 |
| 想改端口 | 修改 docker-compose.yml 的 `3000:3000` 左边数字，如 `8080:3000` |
| 更新了代码 | 重新上传 → `docker compose up -d --build`（数据库不受影响） |
