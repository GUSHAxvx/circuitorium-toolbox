# CIRCUITORIUM 工具箱路线图（本地优先改造）

> 目标：把现在的"服务器网站"改造成**能拷贝、能传播的本地电子工具箱**——不依赖服务器、不需要 API Key 也能完整使用。

## 一、验收标准（你定的）—— 逐条实测结果

| 验收项 | 结果 | 证据（怎么验的） |
|---|---|---|
| 断网能用 | ✅ | 便携版断网后打开作品、改项目描述、加元件（6 → 7 种），控制台 0 报错；桌面版根本不需要网络 |
| 无 Key 能分享 | ✅ | 全程未配置任何识别凭据：分享页 12,242 字节、作品码、作品文件 3,134 字节均正常生成 |
| 分享不含 Key | ✅ | 作品文件（解压逐条查）与分享页内嵌字节里都搜不到 Key / 令牌 / 配置项；manifest 显式声明 `excludes: 本机设置、识别凭据、访问令牌、本机文件路径` |
| 解压即用 | ✅ | 便携版双击 `启动.bat` 0.8 秒可用；桌面版双击 exe 直接开窗（另有安装包：装到当前用户、带开始菜单与卸载项，已实测安装+卸载干净） |
| 导入能改再分享 | ✅ | 桌面版：导出 → 原生「另存为」落盘 3,018 字节 → 「打开作品」选回该文件 → 8 元件与描述原样还原、标记「收到的作品」→ 做同款生成副本、原件不动 |
| 学生界面不出现「API Key / JSON / 导入导出 / 项目包」等词 | ✅ | 七个界面（作品列表 / 作品详情 / 分享弹层 / 识别弹层 / 识别设置 / 备份弹层 / 新建项目）全文扫描 14 个敏感词，**0 命中**；分享页正文同样 0 命中 |

> 另外两条"实现口径"说明：
> - **本地存储**：浏览器版与桌面版都用 **IndexedDB**（桌面版跑在 WebView2 里，origin 是 `http://tauri.localhost`，是真实来源，实测写入落盘、刷新后数据都在）。原先设想的"Tauri 用 SQLite"没有做——数据留在浏览器存储里已经满足"断网可用 + 拷走 exe 不丢数据"，而且存储层在 `ToolboxStore` 接口后面，将来要换 SQLite 只加一个实现、界面代码不用动。
> - **桌面版数据位置**：`%LOCALAPPDATA%\com.circuitorium.toolbox`（Windows 应用的标准位置，约 34 MB）。删掉 exe 不丢数据；作品要跟着人走就用「分享作品 → 作品文件」带 `.ecp`。

## 二、两条硬约束（实测得出，直接影响方案）

### 1. Next.js 的页面跳转本身就要联网

实测：把网络切断后，点项目卡片触发 `router.push('/toolbox/project/xxx')`，控制台报
`Failed to fetch RSC payload ... Falling back to browser navigation`，跳转失败。

原因：App Router 的每次路由切换都要向服务器取该路由的 RSC 数据（连查询参数变化也会取）。

**结论**：本地版**不能靠路由切换来承载"打开某个项目"**。两条可行路线，Tauri 阶段二选一：

- A（现行做法）：单路由 + 视图切换——工具箱里点项目是在同一页面内换视图，零网络请求；
- B：静态导出（`output: 'export'`）后每个路由都是本地文件，或把动态路由改成查询参数（`/toolbox?p=<id>`），配合 Tauri 的本地资源协议。

### 2. `file://` 下 IndexedDB 不可靠

Chrome 把 `file://` 当 opaque origin，IndexedDB 直接不可用。

**结论**："双击 index.html 就能存项目"的纯网页便携版**技术上不成立**；便携版必须是
Tauri（真实 origin + 本地资源）或自带本地服务。→ 你选的 **Tauri 桌面版为主**是正确的。

## 三、分阶段计划

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 本地数据核心 + 最小可用闭环（新建/模板建项目/手动加元件/传图/保存） | ✅ 完成 |
| 1 | 把现有页面（首页、精选项目页）接到本地数据：加一层"数据源"适配，服务器页保持可用直到本地版覆盖完 | ✅ 完成（`ProjectDetailView` 支持 `mode="local"`，本地与服务器共用同一套「项目页」） |
| 2 | `.ecp` 作品文件 + 自包含分享页 + 作品码二维码 | ✅ 完成 |
| 3 | AI 可选：手动模式永远可用；本机配置；老师配置码 + 云函数代理（真 Key 在环境变量） | ✅ 完成 |
| 4 | 便携版打包（**已可"解压即用"**）；Tauri 安装包待工具链 | ✅ 完成：便携版 + **Tauri 桌面版 exe / 安装包**（工具链装在 D 盘，见「四之十」） |
| 5 | 示例项目 / 作品墙（本地） | ✅ 完成 |
| 6 | 真实使用加固：图片自动压缩、整机备份与恢复 | ✅ 完成 |

## 四之十、Tauri 桌面版（阶段 4 收尾，2026-09-24）

### 结论先看

| 产物 | 位置 | 体积 |
|---|---|---|
| **便携版 exe**（双击即用） | `桌面版\CIRCUITORIUM工具箱.exe` | 3.62 MB |
| **安装包**（开始菜单 + 卸载项） | `桌面版\CIRCUITORIUM工具箱-安装包.exe` | 1.65 MB |

原来的便携版（`便携版\启动.bat`，需要一个最小化的 PowerShell 服务窗口）仍然保留，两条路都能走。

### 工具链：其实只差 Rust

开工前先查了一遍本机，发现**重量级前置已经有了，而且都在 D 盘**：

| 需要的东西 | 实际情况 |
|---|---|
| Node / npm | ✅ 已装 |
| WebView2 运行时 | ✅ 已装（桌面版界面靠它） |
| **MSVC C++ 生成工具 + Windows SDK** | ✅ **已装**：VS 2022 Community 在 `D:\C`，Windows SDK 10.0.22621 在 `D:\Windows Kits`（共 7.5 GB，本来就在 D 盘） |
| **Rust 工具链** | ❌ 需要装 → 这次装上，**全部落在 D 盘** |

所以**没有**再下载那 6–8 GB 的 VS Build Tools。

### 网络：国内环境要换镜像

实测这台机器：`static.rust-lang.org`、`sh.rustup.rs` **DNS 解析失败**，`github.com` 连接超时，`crates.io` 同理；npm 正常。所以：

- rustup 安装包与工具链走 **清华 TUNA**：`mirrors.tuna.tsinghua.edu.cn/rustup`
- crates.io 走 **中科大 USTC 稀疏索引**：写进 `D:\rust\cargo\config.toml`
- npm 依赖（`@tauri-apps/cli`、`@tauri-apps/api`）走默认源即可
- 有意思的是：Tauri 打安装包时要下载 NSIS（GitHub Releases），**这次居然通了**，所以安装包也做出来了

### 装到哪了（C 盘几乎没动）

| 内容 | 位置 | 体积 |
|---|---|---|
| Rust 工具链 | `D:\rust\rustup` + `D:\rust\cargo` | 0.93 GB |
| 构建中间产物（target） | `D:\rust\target\circuitorium` | 1.54 GB（可随时删，重编会自动重建） |
| Tauri 的 npm 包 | 项目 `node_modules\@tauri-apps` | 20 MB |
| 桌面版数据（WebView2） | `C:\Users\<用户>\AppData\Local\com.circuitorium.toolbox` | 30 MB |

系统环境变量（新开的终端自动生效）：`RUSTUP_HOME`、`CARGO_HOME` 指向 D 盘，`PATH` 里加了 `D:\rust\cargo\bin`。

### 顺手修掉一个真问题：桌面版里「保存作品文件」不落盘

浏览器里 `<a download>` 能下载，但**在 WebView2 里不会真的存文件**（没有浏览器的下载界面）。
实测：zip 生成完全正常（3,017 字节、PK 头正确），但磁盘上什么都没有 —— 这会让"作品文件传播链"在桌面版直接断掉。

修法：加了一个 Rust 命令 `save_work_file`，桌面版里改成弹 **Windows 原生「另存为」**（默认文件名、`.ecp` 过滤器、用户自己选位置），浏览器版维持原来的下载行为。前端收敛成一个 `saveBlob()`（`src/lib/saveFile.ts`），四处调用点（保存作品文件 / 分享页 / 备份包 ×3）都换过来了，提示文案会显示"保存在哪个文件夹"。

### 代码与命令

```
src-tauri\Cargo.toml            Rust 侧依赖（tauri 2 / tauri-plugin-dialog / base64）
src-tauri\src\lib.rs            开窗 + save_work_file 命令
src-tauri\tauri.conf.json       窗口 1360×900、frontendDist 指向 便携版\site、NSIS 中文安装包
src-tauri\capabilities\default.json
src-tauri\tauri.verify.conf.json  验证用配置：临时开 WebView2 远程调试端口（正式版不含）
工具\build-desktop.mjs         一条命令：静态站点 → 编译 exe → 报告产物
工具\browser-check.cjs         新增 --attach（连桌面版的 WebView）、--file-input（把真实文件塞给 file input）
```

```powershell
npm run build:desktop              # 出便携版 exe
npm run build:desktop-installer    # 再打一个安装包
```

### 实测结果（真实桌面窗口，用 CDP 附着到 WebView2 验的）

| 验证项 | 结果 |
|---|---|
| 编译 | 通过（首次 3 分 03 秒，之后增量 1 分半） |
| 桌面窗口打开 | 通过（标题「CIRCUITORIUM 工具箱」，自带图标，窗口 1376×939） |
| 页面来源 | `http://tauri.localhost/toolbox/`（真实 origin，IndexedDB 可用） |
| 界面与网页版一致 | 通过（作品墙、示例作品、模板全在；控制台 0 报错） |
| **本地数据落盘** | 通过（`EBWebView\...\IndexedDB\http_tauri.localhost_0.indexeddb.leveldb` 有数据） |
| 打开作品 → 改描述 → 加元件 | 通过（7 → 8 种） |
| **刷新后数据还在** | 通过（描述与元件都保留） |
| **保存作品文件（原生另存为）** | 通过（弹出「保存到…」对话框 → 落盘 3,018 字节、PK zip 头正确） |
| 导出内容检查 | 通过（manifest/components/sections/images/README 齐全；作品名、描述、8 元件、7 教程；**不含任何凭据**，manifest 声明 excludes） |
| **打开作品文件（真实文件）** | 通过（列表 3 → 4 件，标记「收到的作品」，8 元件、描述还原） |
| 打开后可改、可再分享 | 通过（做同款生成《…（我的副本）》，列表 5 件，原件不动） |
| 安装包 | 通过（静默安装 → 装到 `%LOCALAPPDATA%\CIRCUITORIUM Toolbox`、建开始菜单快捷方式、注册卸载项；启动正常；静默卸载后目录/快捷方式/注册表项全部清干净） |
| 正式版没有调试后门 | 通过（远程调试端口只在 `tauri.verify.conf.json` 里，正式构建不含） |

> 换电脑/给学生的使用说明写在 `桌面版\使用说明.txt`。
> 数据落在 `%LOCALAPPDATA%\com.circuitorium.toolbox`（Windows 应用的标准位置）：**删掉 exe 不丢数据**；作品要跟着人走就用「分享作品 → 作品文件」带 `.ecp`。

## 四之六、Tauri 打包的前置条件（待你确认再装）

本机检查结果：

| 需要的东西 | 现状 |
|---|---|
| Node / npm | ✅ 已装 |
| WebView2 运行时 | ✅ 已装（Tauri 运行界面靠它） |
| **MSVC C++ 生成工具（VS Build Tools）** | ❌ **没有** → 需要安装，约 4–6 GB |
| **Windows SDK** | ❌ 没有 → 随 VS Build Tools 一起装 |
| **Rust 工具链（rustup + cargo）** | ❌ 没有 → 约 1.5 GB |

合计约 **6–8 GB 下载**，安装时间视网速 30–60 分钟（可能需要重启）。装好后 `npm run tauri build` 就能产出 `工具箱.exe`（双击即用、无 PowerShell 窗口、数据写到 exe 旁的 data 目录）。

> 在装工具链之前，**现在的便携版已经能满足"解压即用"**：唯一差别是它需要一个最小化的 PowerShell 服务窗口（可以最小化，关掉才停止服务），而不是一个纯 exe。要现在就装工具链的话说一声，我在后台装。

## 四之七、阶段 5：示例作品 + 作品墙

**为什么做**：新人第一次打开工具箱时是空的，容易不知道能做什么。现在首次运行会自动写入 **3 件示例作品**，一打开就能看到"做完是什么样"。

- `src/lib/store/sampleSeed.ts`：3 件示例（LED 呼吸灯入门 / 温湿度计进阶 / 循迹小车挑战），基于内置模板生成，配好简介、详细描述、主要功能，并把前几个元件标成"已备齐"模拟真实进度
- 首次运行写入一次，之后**用户删掉就不会自己回来**（用设置项 `__samples_seeded` 打标记）
- 作品墙：项目卡片加了封面（有图用图，没图用渐变 + 首字），并区分标记：`示例作品` / `来自模板` / `收到的作品`
- 示例作品与收到的作品都提供「做同款」，先复制再改，原件不会被改乱

### 实测结果（全新浏览器配置，首运行）

| 验证项 | 结果 |
|---|---|
| 首次运行自动写入示例作品 | 通过（5 个模板 + **3 件示例作品**，均带「示例作品」标记） |
| 作品墙封面 | 通过（3 张卡片都有封面：无图显示渐变+首字；上传图片后该卡片出现 `blob:` 封面图） |
| 打开示例作品 | 通过（7 个元件、教程节、描述与 4 条主要功能齐全，元件已备齐状态正确） |
| 示例作品的按钮 | 通过（做同款 / 分享作品 / 保存作品文件 / 删除） |
| 做同款后的来源标记 | 通过（副本标记为"自己的作品"，不再误标成"收到的作品"；从别人作品复制仍保留作者） |
| 刷新后数据保持 | 通过（示例与副本都在） |
| 控制台报错 | 0 |

> 顺带修掉一个真 bug：从项目页返回列表时没有刷新摘要，导致**刚上传的图片不会出现在作品墙封面上**（现在返回也会刷新）。

## 四之九、阶段 1：本地版直接用同一套「项目页」（不复制界面）

**为什么这么做**：之前本地版自己做了一个简化的项目视图（`ToolboxProjectView`），结果是"同一种东西两套界面"——服务器版那个好看的详情页（面包屑 / 标题区 / 预览 + 项目信息 / 项目描述 / BOM / 项目图片 / 图文教程 / 注意事项）在本地版里看不到，改一处还要改两遍。

**做法**：给 `ProjectDetailView` 加一个 `mode`（`server` / `local`），**界面一模一样**，只把数据源和动作换成本机的：

| 位置 | server 模式 | local 模式 |
|---|---|---|
| 数据 | `/api/projects/{id}` | `loadLocalProjectView()`（IndexedDB，不联网） |
| 面包屑 | 社区 › 项目详情 | 我的工具箱 › 作品详情（点击返回，走视图切换不发请求） |
| 顶部按钮 | 分享 / 二维码 | 分享作品 · 保存作品文件 · 做同款 · AI 识别元件 |
| ⋯ 菜单 | 复制链接 / 查看分享页 / 取消分享 / 删除项目 | 删除作品 |
| 项目信息 | 作者、发布时间、项目热度 | 来源（本机作品 / 收到自某人）、保存在、元件数、文件大小 |
| 描述与功能 | PUT `/api/projects/{id}` | `localUpdateProject()` |
| BOM 增删改 | `/api/projects/{id}/components/*` | `localAddComponent / localUpdateComponent / localRemoveComponent` |
| 图片 | `/api/projects/{id}/images` | `localAddImage / localRemoveImage`（blob 地址显示） |
| 相关项目推荐 | 显示社区推荐 | 不显示（本地没有社区） |
| 导航 | 全站导航 | 只留「工具箱」+ 本地模式徽标 |

`ToolboxProjectView.tsx` 已删除；AI 识别改成独立的 `LocalAiRecognize.tsx`（没配凭据时给"怎么开"的引导，不挡路）。

### 实测结果（真实浏览器，两套模式都跑）

| 验证项 | local 模式（/toolbox 打开作品） | server 模式（/projects/10，演示账号） |
|---|---|---|
| 页面结构 | 通过（8 张卡片：标题区 / 预览 / 项目信息 / 项目描述 / BOM / 项目图片 / 教程） | 通过（9 张卡片，含相关项目推荐） |
| 面包屑 | 我的工具箱 › 作品详情 | 社区 › 项目详情 |
| 顶部按钮 | 分享作品 · 保存作品文件 · 做同款 · AI 识别元件 | 分享 · 二维码 |
| 项目热度 | 不显示（改为「保存在 / 来源」） | 显示（11 次浏览） |
| 项目描述可自行编辑 | 通过（写入 → 保存 → **刷新页面后仍在**） | 通过 |
| 手动添加元件 | 通过（使用元器件 7 种 → 8 种，刷新后新元件仍在） | —（服务器版走 AI 识别页） |
| 做同款 | 通过（生成《…（我的副本）》，作品 3 → 4 件，原件不动） | 通过（导入项目） |
| 保存作品文件 | 通过（3,133 字节，PK zip 头正确，含 manifest/components/sections/images/README） | 通过 |
| **作品文件不含凭据** | 通过（文件里搜不到 token / Key / 配置项） | 通过 |
| **断网后继续用** | 通过（断网状态下列表仍打开作品、改描述、加元件：6 → 7 种，控制台 0 报错） | — |
| AI 识别入口（未配凭据） | 通过（提示"没配置也没关系，可以手动录入"，给「去设置识别凭据」） | — |
| 控制台报错 | 0 | 0 |

> 验证工具顺手加了两个能力：`--script/--script2`（断言写在 UTF-8 的 JSON 文件里，中文不再经命令行传参乱码）与 `--click/--click2`（真实鼠标事件，用于验证下载这类需要用户手势的动作）。可复用的断言脚本都放在 `工具/checks/`（用法见那里的 README）。

### 顺带：服务器版的「项目」列表页也统一了样式

原来的 `/projects` 是一列朴素的条目（和详情页不像一套东西）。现在改成和详情页一致的卡片网格：标题区（我的项目 + 新建项目）、每件作品一张卡（首字图标、名称、说明两行截断、元件数与更新时间、「打开 →」「删除」），空状态与新建表单也统一了。功能一个没少：新建（含首页 `?new=1` 直接展开）、删除、点开进详情，均已实测通过（0 控制台报错）。

## 四之八、真实使用加固（图片压缩 + 整机备份）

两件"学生真的用起来才会暴露"的事：

### 1. 图片自动压缩（`src/lib/images.ts`）

学生用手机拍照动辄 3–9 MB，原样存本地既占空间，也会让作品文件大到发不出去。现在存图前会自动处理：

- 最长边超过 1600px 或体积超过 400KB → 等比缩到 1600px 内并转 JPEG(q0.85)
- 小图 / 动图 / 矢量图**原样保留**（压了会丢动画或变模糊）
- 压完反而更大就保留原图；浏览器解不开的格式直接原样存

### 2. 整机备份与恢复（`src/lib/backup.ts` + `BackupModal`）

工具箱 → 「备份与恢复」：

- **备份全部作品**：把每件作品打成一个 `.ecp`，连同 `manifest.json` 装进一个 zip（文件名 `工具箱备份-YYYYMMDD.zip`）
- **从备份恢复**：选压缩包，逐件导入（每个都走作品文件那套校验），不会覆盖现有作品
- 同名作品自动加序号，避免互相覆盖
- 上限保护：条目数 ≤500、体积 ≤512MB；备份里只有作品，不含凭据与本机设置

### 实测结果（真实浏览器）

| 验证项 | 结果 |
|---|---|
| 大图自动压缩 | **9.6 MB → 1.0 MB**（2000×1400 噪声图，最坏情况；真实照片会更小） |
| 本机占用随之更新 | 通过（界面显示「图片已占用 1 KB → 1.0 MB」） |
| 生成备份 | 通过（`工具箱备份-20260923.zip`，3 件作品，9,801 字节） |
| 从备份恢复 | 通过（提示「已找回 3 件作品」，作品 3 → 6 件，名称一致） |
| 控制台报错 | 0 |

## 四之五、便携版：解压即用（阶段 4 第一步）

**为什么先做这个**：Tauri 在 Windows 上编译需要 MSVC C++ 工具链 + Windows SDK（本机没有，需装 VS Build Tools 约 4–6GB）再加 Rust（约 1.5GB）。先用一条不依赖任何工具链的路把"解压即用"落地。

**产物**：`便携版/`（1.32 MB，58 个文件）

```
启动.bat        双击这个（会自动等本地服务就绪再打开浏览器）
server.ps1      本地静态服务：用 Windows 自带的 PowerShell TcpListener 实现
site/           纯静态工具箱（Next 静态导出，只有 toolbox 一个页面）
使用说明.txt     给使用者看的说明
```

用法：把整个 `便携版` 文件夹拷给别人（U 盘/网盘/微信）→ 对方双击 `启动.bat` → 浏览器自动打开工具箱。
不需要安装 Node、不需要联网、不需要管理员权限；数据存在浏览器为 `http://127.0.0.1:8734` 保存的本地数据里。

**重新打包**：`npm run build:portable`（在隔离副本里构建，不影响正在跑的 dev server；服务器版 `npm run build` 不受影响）

### 两个必须记住的坑（都已修）

1. **用户数据不能打进分发出去的包**：第一次打包把 `public/uploads` 里 38 张用户图片（33.7 MB）一起打进去了 —— 既是隐私问题也是体积问题。现在构建脚本显式排除 `uploads / data.db`，体积从 35 MB 降到 1.32 MB。
2. **PowerShell 脚本必须带 UTF-8 BOM**：不带 BOM 时 Windows PowerShell 按 GBK 解析，中文注释会把字符串引号"吃掉"，脚本直接语法错误（server.ps1 就踩了这个）。`.bat` 则必须 CRLF，否则 `goto` 标签会失效。

### 实测结果

| 验证项 | 结果 |
|---|---|
| 静态导出构建 | 成功，只导出 `/toolbox`、`/manifest.webmanifest`、`/_not-found` 三个路由 |
| 服务器版构建不受影响 | 通过（条件导出，`npm run build` 仍正常，路由全 200） |
| 打包是否夹带用户图片 | 已排除（1.32 MB，58 文件） |
| 本地服务：正常资源 | `/toolbox/` 200、`/` 200、图标与 JS 全部 200 |
| 本地服务：缺失文件 | 404 |
| 本地服务：目录穿越（`/..%2f..%2fsecret.txt`） | **403 拒绝** |
| 解压到全新位置 → 双击 `启动.bat` | **0.8 秒页面可访问** |
| 便携版里建项目（用模板） | 通过（5 个模板可选） |
| **刷新页面后数据还在** | 通过（重新打开项目，5 行 BOM 正常显示） |
| 便携版里手动加元件 | 通过（→ 6 行） |
| 控制台报错 | 0 |
| **收作品流程（界面实测）** | 新建小作品 → 生成作品码（1565 字符）→ 粘到「作品码」打开 → 标题正确、标记「收到的作品」、按钮出现**做同款** → 可改可再分享 |
| 自己的项目不该显示「做同款」 | 通过（自己的项目只显示 分享作品 / 保存作品文件 / 删除） |

## 四之三、阶段 3 已完成的内容（AI 可选）

三种模式，**任何一种都能用工具箱的全部功能**（识别只是可选加速）：

| 模式 | 谁能用 | 凭据放在哪 |
|---|---|---|
| 手动录入（默认） | 所有人，零配置 | 不需要凭据 |
| 本机配置 | 自己申请 Key 的人 | 只存本机 IndexedDB，不上传 |
| 老师配置码 | 学生扫码即用 | 配置码里只有代理地址 + 访问令牌 + 模型名，**没有真 Key** |

代码：

- `src/lib/ai/config.ts`：模式与配置读写、配置码编解码（`CT1-` + base64url）、切回手动模式自动清除凭据
- `src/lib/ai/recognize.ts`：OpenAI 兼容的视觉识别调用、结果清洗、`AiError` 分类（无配置/网络/凭据/返回错误/超时）与中文友好文案、轻量连通性测试
- `src/components/AiSettingsModal.tsx`：识别设置弹层（三种模式 + 本机配置 + 学生粘贴码 + 老师生成码与二维码）
- `src/components/ToolboxProjectView.tsx`：项目页「拍照识别元件」（手机端直接调起相机），识别结果直接落本地库
- `工具/worker-proxy.js` + `代理部署说明.md`：Cloudflare Worker 代理模板与老师部署步骤（真 Key 放 Worker 环境变量，换令牌即失效）
- `src/lib/sanitize.ts`：作品文件与 AI 返回共用的清洗/限幅与 JSON 抽取

### 实测结果（真实浏览器，16 项逻辑 + 10 项界面，全通过）

| 验证项 | 结果 |
|---|---|
| 默认为手动模式、无需任何配置 | 通过 |
| 老师配置码生成/解析 | 通过（114 字符，含二维码） |
| 配置码里没有真 Key | 通过 |
| 乱码配置码被拒绝 | 通过 |
| 学生应用配置码 → 保存本机 | 通过 |
| 连通性测试 | 通过 |
| 识别返回结果并写入项目 | 通过（LM358 + 10kΩ 电位器） |
| 令牌失效提示 | 「配置可能已失效，请向老师要新的配置码」 |
| 连不上提示 | 「检查网络，或改用老师给的配置码」 |
| **配了 AI 后导出的作品文件不含 Key / 令牌 / 配置项** | 通过（解压后逐条检查） |
| 切回手动模式清除凭据 | 通过 |
| 界面：设置弹层三种模式、默认手动 | 通过 |
| 界面：生成配置码 + 二维码渲染 | 通过 |
| 界面：学生粘贴码 → 应用成功提示 | 通过 |
| 界面：状态显示「识别设置 · 老师配置码」 | 通过 |
| 界面：项目页出现「拍照识别元件」 | 通过 |
| 界面：真实文件输入 → 识别 → 元件 6 → 8 | 通过 |

> 说明：本次验证用的是**临时模拟识别服务**（验证后已删除），没有消耗真实 API 配额。

> 本轮（桌面版完成后）复查：默认仍是「手动录入」；切「本机配置」状态正确、凭据字段可填；生成老师配置码 104 字符（`CT1-` 前缀），**码里不含明文 Key / 令牌**；切回手动模式状态复原，工具箱功能照常，0 控制台报错。

## 四、阶段 0 已完成的内容

- `src/lib/store/types.ts`：数据模型 + `ToolboxStore` 接口（浏览器 IndexedDB 与 Tauri SQLite 共用同一接口）
- `src/lib/store/local.ts`：IndexedDB（Dexie）实现——项目 / 元件 / 教程小节 / 图片（二进制存本机）/ 设置 / 模板 / 统计 / 做同款
- `src/lib/store/templateSeed.ts`：内置模板离线种子（5 套模板、34 节教程，从老库导出，首次运行自动写入）
- `src/app/toolbox/page.tsx`：本地工具箱首页（不登录、不联网、不需要 Key）——新建项目、用模板建项目、做同款、删除、图片占用统计
- `src/components/ToolboxProjectView.tsx`：项目视图（同一路由内切换）——改名称 / 简介 / 项目描述 + 主要功能、**手动加元件**、数量增减、备料状态、上传图片（存本机）、教程展示
- `工具/export-templates.cjs`：从 `data.db` 重新生成模板种子
- `工具/browser-check.cjs`：真实浏览器验证工具（CDP，可等 IndexedDB、可模拟断网）——以后每个阶段的验收都靠它

### 实测结果

| 验证项 | 结果 |
|---|---|
| 本地存储自检（11 项：开库 / 模板 / 建项目 / 加元件 / 存图 / 描述 / 统计 / 做同款 / 模板建项目 / 清理 / 设置） | 全部 PASS |
| 断网 + 用模板建项目 + 手动加元件 | 通过（元件 6 → 7 行，控制台 0 报错） |
| 工具箱页面 | `/toolbox` 200，无 API 依赖 |

## 四之二、阶段 2 已完成的内容（作品文件 .ecp）

zip 结构：

```
manifest.json     作品信息：格式版本、作者、时间、统计、excludes（声明不含密钥）
components.json   元件清单
sections.json     图文教程小节
images.json       图片索引（文件名 / 分类 / 说明）
images/<n>.<ext>  图片二进制（原样存，不二次压缩）
thumbnail.jpg     封面缩略图（自动生成，失败则跳过）
README.txt        给人看的说明（用工具箱打开即可）
```

代码：

- `src/lib/ecp/format.ts`：格式定义、校验、大小/条数上限、文本清洗、图片 magic bytes 识别
- `src/lib/ecp/io.ts`：`exportProjectToEcp()` 导出、`importEcpToStore()` 打开、`downloadBlob()` 落盘
- 界面：「导出作品」在项目视图；「打开作品」在工具箱首页（选文件即打开，并标记为**收到的作品**）
- `store.importBundle()`：批量写入（保留原作者与时间）

### 实测结果（真实浏览器，14 项全 PASS）

| 验证项 | 结果 |
|---|---|
| 导出作品文件 | 4KB，文件名 `呼吸灯（导出测试）.ecp` |
| 文件结构 | README / components / images / manifest / sections / thumbnail 齐全 |
| **作品文件不含本机密钥** | 通过（本机放了假密钥，导出文件里搜不到） |
| manifest 声明 excludes | 通过 |
| 打开作品文件 | 名称、作者（肆）都还原 |
| 元件 / 教程 / 图片还原 | 6 / 7 / 1 全部一致 |
| 标记为「收到的作品」 | 通过 |
| 打开后做同款 | 通过（副本 6 个元件，可自由修改） |
| 副本再改再导出 | 通过 |
| **拒绝路径穿越文件** | 通过（`../evil.txt` 被拒） |
| 拒绝非作品文件 | 通过（提示「这不是 CIRCUITORIUM 作品文件」） |
| 损坏文件提示 | 通过（「作品信息损坏，无法打开」） |
| 界面导出（点按钮） | 通过（生成 `LED 呼吸灯.ecp` 下载，blob 地址正确） |
| 界面打开（真实文件输入） | 通过（导出 2854B → 打开 → 6 个元件 → 列表出现「收到的作品」标记） |

### 阶段 2 剩余

- 二维码分享：作品文件通常几百 KB～几 MB，塞不进二维码；二维码用于**小体量分享页**（数据放 URL hash）或分享链接
- 自包含分享页（单个 HTML，双击就能看，不装工具箱也能预览作品）
- 拖拽 .ecp 到窗口即打开；系统里双击 .ecp 用工具箱打开（Tauri 阶段做文件关联）

## 四之四、阶段 2 收尾：传播链已闭环

三种分享方式（项目页「分享作品」）：

| 方式 | 产物 | 适合 |
|---|---|---|
| **分享页** | 单个 HTML 文件（内嵌作品文件） | 发给任何人，**双击就能看**，页面里点一下就能存进自己的工具箱；不装工具箱、不联网也能看 |
| **作品码** | `CTW1-…` 一串码 + 二维码 | 体量小的作品（纯文本约 2KB 以内）投屏扫码，学生粘到工具箱即可打开 |
| **作品文件** | `.ecp` 文件 | 大作品、要长期存档 |

实测评测（真实浏览器，13 项逻辑 + 4 项 file:// 独立打开）：

| 验证项 | 结果 |
|---|---|
| 生成分享页 | 12KB HTML，命名《作品名-分享.html》 |
| 分享页内嵌作品文件 | 通过（学生可一键保存） |
| 分享页含项目名/作者/元件/教程/图片 | 通过（图片以 data URL 内嵌） |
| **学生看到的文字里没有禁用词** | 通过（检查 API Key / JSON / 项目包 / 导入 / 导出 / 密钥 等 9 个词） |
| 大作品不生成作品码 | 通过（提示改用文件/分享页） |
| 小作品生成作品码 | 通过（1756 字符，二维码装得下） |
| 作品码还原 / 乱码被拒 | 通过 |
| 扫码得到的作品可打开 | 通过（名称、作者、元件都在） |
| 打开后可以改（做自己的版本） | 通过 |
| **file:// 独立打开分享页** | 通过（标题、6 元件行、7 教程节、1 图片，0 控制台报错） |
| **独立页点「保存到我的工具箱」** | 通过（下载《呼吸灯（分享测试）.ecp》，2984 字节，PK zip 头正确） |

> 完整链条：老师生成分享页/作品码 → 学生（哪怕没装工具箱）双击 HTML 看完 → 一键存下作品文件 → 在工具箱「打开作品」→「做同款」→ 改成自己的 → 再分享给下一个人。

## 五、安全清单（阶段 2/3 落地）

- 导入 `.ecp`：解压路径穿越防护、条目数量与总大小上限、manifest/JSON 结构校验
- 文本转义防 XSS（作品名、作者、步骤内容都可能来自别人）
- 导出时**自动剔除设置项**（API Key、代理 token 一律不写入作品文件）
- 分享页（自包含 HTML）不内嵌任何密钥

## 六、当前状态说明

- **三条路都能用，随便挑**：
  1. **桌面版**（给老师/学生）：`桌面版\CIRCUITORIUM工具箱.exe`（3.62 MB，双击即用）或安装包
  2. **便携版**（U 盘 / 网盘传）：`便携版\启动.bat`（1.38 MB，绿色免安装，需要一个最小化服务窗口）
  3. **服务器版**（课堂演示 / 多人）：`npm run dev` 后开 `http://127.0.0.1:3000`，项目/社区/模板/AI识别都在
- 服务器版**一点没动**：`/projects`、`/community`、`/share-project/*`、演示账号一键登录全部实测正常（本轮复查：所有路由 200、演示项目页 9 张卡齐全、0 控制台报错）
- 本地版与服务器版**共用同一套「项目页」**（`ProjectDetailView` 的 `server` / `local` 两种模式），不会再出现"两套界面各改一遍"
- 验证脚本都收在 `工具/checks/`（用法见那里的 README），新加的能力：`--attach`（连桌面版 WebView 测真实窗口）、`--file-input`（把真实文件塞给文件输入框）、`--script/--script2`（中文断言不乱码）、`--click/--click2`（真实鼠标事件）

### 还没做、可以再排的（都不是验收项）

- 桌面版换 SQLite 存储（当前用 WebView2 的 IndexedDB，见开头说明）
- 桌面版数据目录改到 exe 旁边（真正的"数据随身走"）
- `.ecp` 文件关联（系统里双击 .ecp 直接用工具箱打开）、拖拽 .ecp 到窗口
- 代码签名（免 SmartScreen 提示）、自动更新
- 作品码装大作品的方案（当前超 2KB 会提示改用分享页/作品文件）
